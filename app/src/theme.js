import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        background: {
            default : "#fdfcfd"
        },
        primary: {
            main: "#003B33"
        },
        secondary: {
            main: "#FDF41B"
        },
    },
})

export default theme;